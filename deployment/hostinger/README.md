# Hostinger deployment

Point the domain document root at the release's `public/` directory. Confirm PHP 8.3+, `pdo_pgsql`, queue worker support, cron, writable `storage`/`bootstrap/cache`, SSH/Composer availability, and environment variable management.

Review the examples before use. They contain no credentials and intentionally do not perform an automatic deployment.
